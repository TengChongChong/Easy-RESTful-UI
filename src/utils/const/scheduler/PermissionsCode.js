/**
 * 定时任务 权限标识
 */
export const SCHEDULER_PERMISSIONS_CODE = {
  // 执行日志
  JOB_LOG_SELECT: 'scheduler:job:log:select',
  // 删除
  JOB_REMOVE: 'scheduler:job:remove',
  // 保存
  JOB_SAVE: 'scheduler:job:save',
  // 定时任务
  JOB_SELECT: 'scheduler:job:select'
}
